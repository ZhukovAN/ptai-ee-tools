{
	// Main Settings
	// Наименование проекта под которым он будет отображаться
	// в интерфейсе ПО Application Inspector Viewer
	"ProjectName": "APP01.JSON",
	// Язык программирования, используемый ядром абстрактной интерпретации.
	// Возможные значения - Php, Java, CSharp. В случае анализа кода
	// на языках, не поддерживаемых ядром абстрактной интерпретации, данный
	// атрибут может содержать любое значение из числа указанных выше, а
	// в параметре ScanAppType необходимо отключить использование указанного типа ядра
	"ProgrammingLanguage": "Java",
	// Перечень ядер PT AI, используемых для анализа кода. Помимо приведенных
	// поддерживаются значения CSharp, PHP и BlackBox.
	// Указанные значения соответствуют:
	// Java, PHP и CSharp - ядра абстрактной интерпретации, обеспечивающие
	// максимально достоверные результаты, формирующие эксплойты для проверки
	// наличия уязвимостей, но требующие существенного времени для анализа
	// Configuration - ядро анализа конфигурационных файлов веб-сервера и/или
	// сервера приложений
	// Fingerprint - ядро анализа сторонних библиотек (например, jQuery),
	// используемых приложением на предмет наличия известных уязвимостей.
	// В т.ч. поддерживаются пользовательские Yara-правила
	// PM - ядро выявления уязвимостей методом поиска по шаблонам
	// BlackBox - ядро поиска уязвимостей методом динамического анализа,
	// для работы необходимо указать адрес сайта и параметры аутентификации
	"ScanAppType": "Java,Configuration,Fingerprint,PM",
	// Количество потоков сканирования кода. Время сканирования сокращается
	// с увеличением количества потоков, однако необходимо учитывать требования
	// к оперативной памяти
	"ThreadCount": 1,
	// Адрес приложения, развернутого на тестовой площадке. Используется для
	// формирования эксплойтов, пригодных к запуску и для выполнения
	// динамического анализа
	"Site": "http://localhost",
	// Качество результатов анализа кода тесно связано с полнотой анализируемого
	// кода: в идеале анализируемый код должен быть пригоден для запуска,
	// т.е., содержать все требуемые сторонние библиотеки и фреймворки.
	// PT AI может автоматически загрузить указанные зависимости для проектов,
	// реализованных с использованием следующих менеджеров зависимостей:
	// Composer (PHP), Maven/Gradle (Java) и NuGet (C#).
	"IsDownloadDependencies": true,
	// Включить режим поиска от public/protected методов, реализуемый ядрами
	// абстрактной интерпретации Java/PHP/C#.
	// Позволяет находить уязвимости в коде, использующем не поддерживаемые
	// фреймворки, а также находить потенциальные, т.е., на текущий момент
	// непригодные для эксплуатации уязвимости
	"IsUsePublicAnalysisMethod": false,
	// Включить режим поиска от точек входа, реализуемый ядрами абстрактной
	// интерпретации Java/PHP/C#. Позволяет находить уязвимости, которые могут
	// быть использованы злоумышленниками и формировать эксплойты для демонстрации
	"IsUseEntryAnalysisPoint": true,
	// Максимальное время в секундах, выделяемое на анализ одиночного файла
	// ядрами абстрактной интерпретации Java/PHP/C#. При превышении указанного
	// времени анализ файла прерывается с выводом соответствующего сообщения
	"ScanUnitTimeout": 600,
	// Учитывать результаты предыдущего анализа проекта с тем же именем.
	// Значение true означает, что если в коде по итогам анализа выявлены
	// те же уязвимости, что и при предыдущем анализе, то для них автоматически
	// будут проставлены те же самые статусы подтверждения. Эта опция
	// позволяет сделать процесс анализа сходящимся во времени когда
	// максимальные трудозатраты на разбор уязвимостей приходятся на самые первые
	// итерации анализа
	"ConsiderPreviousScan": true,
	// Типы проверок исходных кодов, которые не будут выполняться в ходе анализа
	"DisabledTypes": [],
	// Расширения имен файлов, которые не будут проверяться в ходе анализа.
	// По умолчанию PT AI пытается найти код в файле с любым расширением
	"SkipFileFormats": ["*.gif"],
	// Каталоги, содержимое которых не будет анализироваться в ходе анализа
	"SkipFilesFolders": ["\\.git\\", "\\.gitignore", "\\.gitmodules", "\\.gitattributes", "\\$tf\\", "\\$BuildProcessTemplate\\", "\\.tfignore"]
}
